# 의존성 4개. 선택 이유는 README '의존성' 절에 한 줄씩 기재.
pydantic>=2.5    # 입력/출력 스키마 검증 - 밴드 재계산 대조를 모델 검증기로 강제
jinja2>=3.1      # HTML 리포트 템플릿 - 문자열 조립보다 화면 구조가 코드에서 읽힘
openai>=1.40     # OpenAI 호환 클라이언트 - base_url 교체만으로 Ollama 겸용 (--api 모드에서만 필요)
pytest>=8.0      # LLM 호출 없이 도는 결정론 테스트 (tests/)
